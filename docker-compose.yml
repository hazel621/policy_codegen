version: '3.8'

services:
  codegen:
    build:
      context: .                # 👈 根目录作为构建上下文
      dockerfile: codegen/Dockerfile
    container_name: codegen
    expose:
      - "8000"
    networks:
      - backend
  nginx-gateway:
    build: ./api-gateway
    container_name: nginx-gateway
    ports:
      - "8080:80"
    depends_on:
      - codegen
    networks:
      - backend

networks:
  backend:
